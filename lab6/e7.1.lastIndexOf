// vim: set ft=java sw=4 ts=4 :

public static int lastIndexOf(int[] arr, int num) {
    int result = -1;
    for (int i = 0; i < arr.length; i++) {
        if (arr[i] == num) {
            result = i;
        }
    }
    return result;
}
